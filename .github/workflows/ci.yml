name: CI Elementares

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  validacao-codigo:
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false 
      matrix:
        service: 
          - alertas-notificacoes
          - analise-dados
          - auth-service
          - gestao-estacoes
          - ingestao-dados
          - servico-relatorios
          - servico-validacao

    defaults:
      run:
        working-directory: ./services/${{ matrix.service }}

    steps:
    - name: Fazer o Checkout do código
      uses: actions/checkout@v4

    - name: Configurar Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24.x'

    - name: Instalar dependências (${{ matrix.service }})
      run: npm ci

    - name: Verificar tipagem TypeScript (${{ matrix.service }})
      run: npx tsc --noEmit

    - name: Rodar Linter (${{ matrix.service }})
      run: npm run lint --if-present

    - name: Rodar Testes (${{ matrix.service }})
      run: npm test --if-present

    - name: Fazer o Build (${{ matrix.service }})
      run: npm run build --if-present

  sonarcloud:
    name: Analise SonarCloud
    runs-on: ubuntu-latest
    needs: validacao-codigo
    steps:
      - name: Fazer o Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # TODO: REMOVER '-Dsonar.coverage.exclusions=**/*' ASSIM QUE OS TESTES FOREM IMPLEMENTADOS
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v3.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=elementares-dsm
            -Dsonar.projectKey=ELEMENTARES-DSM_BACKEND-4SEMESTRE
            -Dsonar.sources=services
            -Dsonar.scanner.dumpToFile=none
            -Dsonar.coverage.exclusions=**/*